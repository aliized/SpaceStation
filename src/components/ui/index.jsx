import React from "react";

const index = () => {
  return (
    <div>
      https://blog.webdevsimplified.com/2022-07/react-folder-structure/ we have
      a ui folder which contains all our UI components like buttons, modals,
      cards, etc. We also have a form folder for form specific controls like
      checkboxes, inputs, date pickers, etc.
    </div>
  );
};

export default index;
