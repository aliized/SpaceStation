const development = process.env.NODE_ENV !== "production";

/**
 * Returns the server URL for the current environment.
 * @returns {string} The server URL for the current environment.
 */
// export const SERVER_URL = development
//   ? "http://localhost:9000"
//   : "https://spaceapi.iran.liara.run";

export const SERVER_URL = "https://spaceapi.iran.liara.run"